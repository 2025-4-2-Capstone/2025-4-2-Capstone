# 📦 Capstone Docker 구성 (DB + FastAPI + 백업)

## ✅ 실행 방법

```bash
git clone <레포주소>
cd capstone
cp .env.example .env
docker compose up -d --build

최종 브랜치
UI - UI_dev_v3
Backend - backend_4.5 
